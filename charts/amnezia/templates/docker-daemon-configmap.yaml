{{- if .Values.dockerDaemonConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "amnezia.fullname" . }}-docker-daemon
  labels:
    {{- include "amnezia.selectorLabel" . | nindent 4 }}
data:
  daemon.json: |
{{ .Values.dockerDaemonConfig.content | nindent 4 }}
{{- end }}
