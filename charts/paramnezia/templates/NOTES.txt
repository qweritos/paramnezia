1. Get the service details:
   kubectl get svc -n {{ .Release.Namespace }} {{ include "paramnezia.fullname" . }}

2. Check pod status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

3. This chart runs in privileged mode; ensure your cluster policy allows it.
